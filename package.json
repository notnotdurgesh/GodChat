{
  "name": "nexusmind-monorepo",
  "private": true,
  "description": "Monorepo for NexusMind Chat and Mermaid MCP service",
  "packageManager": "npm@11.11.0",
  "workspaces": [
    "nexusmind-chat",
    "mermaid-mcp",
    "import-api"
  ],
  "scripts": {
    "dev": "npx turbo run dev",
    "build": "npx turbo run build",
    "lint": "npx turbo run lint",
    "install-all": "npm install"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "dependencies": {
    "axios": "^1.13.6",
    "cheerio": "^1.2.0",
    "node-fetch": "^3.3.2",
    "turndown": "^7.2.2"
  }
}
